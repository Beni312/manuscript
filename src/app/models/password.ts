export class Password {
  password: string;
  passwordAgain: string;

  constructor(password: string, passwordAgain: string) {
    this.password = password;
    this.passwordAgain = passwordAgain;
  }
}
